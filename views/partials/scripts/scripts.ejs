<script src="/plugins/jquery/jquery.min.js"></script>
    <!-- jQuery UI 1.11.4 -->
    <script src="/plugins/jquery-ui/jquery-ui.min.js"></script>
    <!-- Resolve conflict in jQuery UI tooltip with Bootstrap tooltip -->
    <script>
      $.widget.bridge('uibutton', $.ui.button)
    </script>
    <!-- Bootstrap 4 -->
    <script src="/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
    <!-- ChartJS -->
    <script src="/plugins/chart.js/Chart.min.js"></script>
    <!-- Sparkline -->
    <!--<script src="plugins/sparklines/sparkline.js"></script>-->
    <!-- JQVMap -->
    <script src="/plugins/jqvmap/jquery.vmap.min.js"></script>
    <script src="/plugins/jqvmap/maps/jquery.vmap.usa.js"></script>
    <!-- jQuery Knob Chart -->
    <script src="/plugins/jquery-knob/jquery.knob.min.js"></script>
    <!-- daterangepicker -->
    <script src="/plugins/moment/moment.min.js"></script>
    <script src="/plugins/daterangepicker/daterangepicker.js"></script>
    <!-- Tempusdominus Bootstrap 4 -->
    <script src="/plugins/tempusdominus-bootstrap-4/js/tempusdominus-bootstrap-4.min.js"></script>
    <!-- Summernote -->
    <script src="/plugins/summernote/summernote-bs4.min.js"></script>
    <!-- overlayScrollbars -->
    <script src="/plugins/overlayScrollbars/js/jquery.overlayScrollbars.min.js"></script>
    <!-- AdminLTE App -->
    <script src="/js/adminlte.js"></script>
    <!-- AdminLTE for demo purposes 
    <script src="dist/js/demo.js"></script>-->
    <!-- AdminLTE dashboard demo (This is only for demo purposes) -->
    <!--<script src="/js/pages/dashboard.js"></script>-->
    <script src="/js/app.js"></script>
    <script type="text/javascript">
      window._csrf = "";
    </script>
    <script src="/dependencies/sails.io.js"></script>
<script>
  $(document).ready(function() {
      $('#formCambiarOficina').on('submit', function() {
        const $btn=$('#botonEnviar')
        $btn.prop('disabled',true)
        $btn.html('<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span> Cargando...');
      });
  });
</script>
<script src="/js/chat.js"></script>
    <script>
      io.socket.on('user_connected', (data) => {
        console.log(data);
        alert(`Usuario ${data.username} está listo para chatear`);
        
      });
      function cambiarOficina(id){
        $.get(
            '/config/oficinas/obtener',
            {id:id},
            function (respuesta) {
                $('#cambiarOficina').modal('show');
                const label = document.createElement('label');
                label.textContent = 'Elige una oficina';
                label.className = 'col-sm-2 col-form-label'; 
                label.htmlFor = 'oficinasCambiar';

                const divSelect = document.createElement('div');
                divSelect.className = 'col-sm-10';

                const select = document.createElement('select');
                select.name = "oficinasCambiar";
                select.id = "oficinasCambiar";
                select.className = "form-control"; 

                respuesta.oficinas.forEach(oficina => {
                  const option = document.createElement('option');
                  option.value = oficina.id;
                  option.text = oficina.nombre;
                  select.appendChild(option);
                });

                const contenedor = document.getElementById('contenedorOficinas');

                divSelect.appendChild(select);
                contenedor.innerHTML = ''; 
                contenedor.appendChild(label);
                contenedor.appendChild(divSelect);
            }
        ).fail(function(res){
            descErrores=descErrores+"No se ha encontrado el puesto";
            document.getElementById('mostrarErroresRutaEditar').innerHTML=descErrores;
            $('#errorsRutaEditar').show();
            return false;
        });
      }
    document.addEventListener('DOMContentLoaded', function() {
      // Verificamos si el socket está listo
      if (window.io && io.socket) {
        // Enviamos un "ping" al servidor para que asocie este socket con la sesión de Passport
        /*io.socket.get('/user/sync-socket', function(data, jwr) {
          console.log('Estado de conexión sincronizado:', data);
        });*/
      }
    });
  </script>

    
<!--<script src="/dependencies/notificaciones.js"></script>-->